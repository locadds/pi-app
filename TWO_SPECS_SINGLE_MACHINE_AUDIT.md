# 小规两份规格单机测试版完成度审计

日期：2026-08-30

对照文件：

- `小规Agent_模板资产化产品改造规格_v1.md`
- `小规Agent_Univer_Office_Surface_开发实施规格_v1.md`

## 结论

本分支已经完成用户本轮要求的两个核心可测能力：

1. 普通 DOCX 不再扁平拼接成一段文字，而是把正文、文字样式、基础表格、页面参数、页眉页脚和浏览器可显示图片导入 Univer 文档模型。
2. 业务字段和需要判断的位置使用真正的黄色文字背景；黄色位置可点击并与右侧业务字段/问题联动。

因此，本分支可以作为最新单机试用版继续验证。但两份规格所定义的完整生产 V1 **尚未完成**，尤其是正式 DOCX 导出、模板发布与第二次复用闭环、20 份格式回归和生产许可门。

## 一、模板资产化规格

| 要求 | 当前状态 | 当前证据或边界 |
|---|---|---|
| 产品对象从技术候选改为业务字段 | 已完成可测切片 | 默认显示“负责部门”等中文业务字段，不向普通用户展示 FIXED/VARIABLE 等内部分类 |
| 未识别正文默认保留 | 已完成可测切片 | 普通正文不再生成一百多个逐段问题，当前真实样本压缩为 7 个业务字段、5 个合并问题 |
| 一个字段关联多个出现位置 | 已完成基础能力 | Field Graph 与 occurrence 定位已进入投影，黄色位置按 occurrence 映射 |
| 默认先看模板草稿 | 已完成可测切片 | 点击“开始/继续复核”直接进入文档草稿和右侧问题，不再进入旧候选卡片列表 |
| 黄色内容与右侧双向定位 | 已完成 | 点击文档黄色内容会选择对应 occurrence；右侧可反向聚焦文档位置 |
| 风险按业务问题聚合 | 已完成基础能力 | 文本框、浮动对象、旧项目图件、扫描附件均按类别合并处理 |
| 字段重命名、类型、必填、默认值、合并和拆分 | 部分完成 | 后端已有字段/位置模型；面向用户的完整字段编辑器尚未全部补齐 |
| 自动验证并生成不可变模板版本 | 部分完成 | 本机工作副本、摘要和保存恢复已具备；正式发布验证与不可变版本晋升尚未封口 |
| 模板库版本、归档、回滚、VERIFIED/STALE | 部分完成 | 既有模板库基础存在；本规格要求的完整生命周期尚未完成 |
| 新会话自然语言调用模板并生成正式 DOCX | 未完成整条旅程 | 已有“按模板生成”自然语言入口和旧流程，但尚未用本次 Field Graph 资产完成端到端第二次复用验收 |

## 二、Univer Office Surface 规格

| 要求 | 当前状态 | 当前证据或边界 |
|---|---|---|
| 独立 Viewer 与插件模式 | 已完成 | Viewer 单独构建；Univer SDK Cohort 固定为 0.25.1 |
| iframe 隔离 | 已完成 | Office Viewer 运行在独立回环来源 iframe，不进入小规主 React 树 |
| 本机回环 Gateway 与临时授权 | 已完成基础能力 | 随机回环端口、临时消息令牌、Snapshot GET/PUT；不向 Viewer 返回源文件路径 |
| DOCX 直接进入 Univer | 已完成单机自研投影 | 不转 PDF；OOXML 直接映射为 Univer `IDocumentData` |
| 正文、基础样式、表格、页眉页脚 | 已完成可测切片 | 复杂真实样本可连续滚轮浏览 |
| 文中图片 | 已完成浏览器图片切片 | 真实样本识别并显示 22 个图片对象；使用 Univer 官方绘图插件和绘图资源 |
| 字段非破坏性视觉标记 | 已完成 | 黄色背景不改原值；点击后发送 occurrence 选择事件 |
| 工作副本保存和恢复 | 已完成基础能力 | Viewer Snapshot 可保存、重载；关闭后可继续复核 |
| 浮动对象和复杂 Word 版式 | 部分完成 | 浮动图片近似定位；文本框位置、复杂环绕、组合图形和部分矢量格式仍需高级检查 |
| 正式 DOCX Exchange 导入/导出 | 未完成 | 当前为自研 OOXML→Univer 投影，没有正式 Univer Pro Exchange，也未开放正式导出 |
| Ready/Reopen/Merge/Discard 完整产品闭环 | 部分完成 | 领域状态机已有；模板复核界面尚未完整接通用户级 Merge/Discard 发布旅程 |
| 20 份真实文档格式回归 Gate D2 | 未完成 | 当前只完成一份复杂 DOCX 可见冒烟，不得据此宣称 Word 像素级一致 |
| 生产许可、离线部署和 Preferred 开关 | 未完成 | 当前只使用 Apache-2.0 开源包，仍是单机实验能力 |

## 三、本次建议用户重点测试

1. 点击“继续复核”，确认无需输入“复核”即可直接进入文档界面。
2. 在第一页确认原文文字、字号/颜色和黄色字段同时存在。
3. 用鼠标滚轮向下翻阅，确认图件和后续正文都可见。
4. 点击黄色文字，确认右侧切换到对应字段或问题；再从右侧项目反向定位。
5. 修改少量文字并保存工作副本，关闭后再次点击“继续复核”。
6. 留意浮动图件、文本框、复杂表格或页眉页脚错位；这些是下一轮格式补偿的主要输入。

## 四、下一施工优先级

1. 完整字段编辑：重命名、类型、必填、默认值、合并、拆分与撤销。
2. 正式模板发布：验证、生成不可变版本、保存模板库。
3. 第二次复用闭环：自然语言检索模板、补齐缺失字段、整份预览、发布正式 DOCX。
4. 格式回归：扩展真实样本，逐项补偿浮动对象、复杂表格、矢量图和文本框。

在以上工作和 Gate D2 完成前，Univer 应继续标记为单机试验能力，不覆盖稳定正式主线。
