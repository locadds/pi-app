# 小规 Office Surface 单机测试说明

## 本版测试什么

本分支把“模板资产化”和“Office Surface”两份规格接入同一条真实 DOCX 流程：

```text
普通 DOCX
→ 安全读取与字段图谱
→ 小规文档工作表面（Univer 结构视图）
→ 右侧业务字段 / 待确认问题
→ 点击字段联动定位
→ 保存本机工作副本并可恢复
```

以下三个位置统一使用 `DocumentSurface` 接缝：

- 模板草稿复核；
- 正式模板生成前预览；
- 本机模板库预览。

工作表面启动失败时会明确切换到“原版式兼容视图”，不会显示空白页冒充成功。

## 如何启动

在本工作树执行：

```powershell
npm run dev:office-test
```

单机测试数据和工作副本放在 `D:\CodexTemp\xiaogui-office-surface-worktrees`，不把大体积缓存写入 C 盘。

## 建议测试步骤

1. 打开一条已经生成模板草稿的 WORK 对话，点击“直接打开文档复核”。
2. 确认默认显示“工作表面（试验）”，能看到真实文档文字，而非空白页面。
3. 切换“业务字段”，点击不同字段，观察文档中的对应内容定位。
4. 切换“待确认”，确认风险问题仍由右侧结构化界面处理。
5. 在工作表面修改少量文字，等待自动保存或点击“保存工作副本”。
6. 关闭复核器后再次打开，确认工作副本可以恢复。
7. 切换到“原版式兼容视图”，核对版式、图片、文本框和浮动对象。

## 当前边界

- 当前 Univer 开源版没有正式 DOCX Exchange，工作表面是可编辑的“DOCX 结构视图”，不宣称与 Word 像素级一致。
- 复杂版式仍以兼容视图核对；它是保底能力，不是默认主工作面。
- 本版不直接覆盖原 DOCX，也未开放正式 DOCX 导出。
- Office 网关只监听本机回环地址；文档快照接口通过消息通道传入的临时令牌授权，令牌不进入 URL、模型上下文或公开工具结果。

## 已完成的聚焦验证

- TypeScript 类型检查；
- 本机网关授权与工作副本重启恢复；
- 真实 DOCX 结构投影与字段位置映射；
- Office Surface 浏览器保存 / 重载冒烟；
- Electron 可见冒烟，并在超过原 15 秒超时窗口后确认未误降级。

