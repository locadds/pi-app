# 小规文档工作表面单机测试说明

## 本版目标

```text
普通 DOCX
→ 安全读取与字段图谱
→ OOXML 投影到 Univer
→ 正文、基础表格、页眉页脚和常见图片显示
→ 字段或风险位置标黄
→ 用户复核并保存本机工作副本
```

模板草稿复核、正式模板生成前预览和本机模板库预览共用一个 `DocumentSurface` 接缝。用户界面中不提供两种 Viewer 的切换。单机试用开关启用时直接显示 Univer；启动失败时自动进入只读 DOCX 回退视图并明确提示。

## 启动方式

在本工作树执行：

```powershell
npm run dev:office-test
```

工作副本默认放在 `D:\CodexTemp\xiaogui-office-surface-worktrees`，不把构建证据和大体积缓存新增到 C、E 盘。

## 建议人工测试

1. 打开已有模板整理报告，点击“开始复核”或“继续复核”。
2. 确认页面只出现一个小规文档工作表面，没有 Viewer 选择按钮。
3. 用滚轮连续浏览正文，检查基础表格、正文图片、页眉和页脚图片。
4. 点击黄色内容并确认右侧项目联动；再从右侧反向定位。
5. 展开“导入提示”，确认无法等价显示的对象逐项可见，而不是只显示第一条。
6. 保存工作副本，关闭后再次打开并确认内容可恢复。
7. 人为关闭 Office 试用开关或制造 Viewer 启动失败，确认自动进入只读回退视图，标黄和选择仍可使用。

## 当前已验证

- 表格正文和页眉页脚子文档都携带自身表格配置。
- 页眉页脚按 DOCX 关系和首页/默认/偶数页规则绑定；多节差异明确提示。
- 浏览器冒烟覆盖正文、表格单元格、页眉和页脚四类图片资源。
- 图片可由浏览器解码，工作副本保存、重载后资源仍在。
- VML 位置、旋转/翻转、可靠裁剪以及 `mc:AlternateContent` 单分支有聚焦测试。
- 不支持的图片格式、组合图形、缺失关系和复杂样式进入可展开的结构化提示。
- Gateway 令牌只保留在 Main/Gateway；Viewer 经 `MessagePort → 父 Renderer → 窄 IPC → Main` 代理读写，普通 Renderer、URL 和消息体均不携带令牌。
- `file://` 嵌入鉴权冒烟确认直接访问为 401、受控代理成功，浏览器 Cookie 数量为 0。
- 同一工作副本拒绝并发打开；损坏快照明确失败。

## 当前边界

- 不宣称与 Microsoft Word 像素级一致，也不提供正式 DOCX 导出。
- 多节页眉页脚只能可靠展示末节集合，其余分节进入人工复核提示。
- EMF、WMF、TIFF、组合图形、OLE、复杂环绕及部分文本框仍可能降级。
- 自动冒烟使用合成的表格与图片文档；真实院内复杂样本仍以用户单机试用为最终体验门。
- 本结果只批准当前功能分支单机试用，不代表已经合入阶段线或发布安装包。
