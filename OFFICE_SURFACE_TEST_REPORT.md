# 小规 Office Surface 单机测试说明

## 本版测试什么

本分支把“模板资产化”和“Office Surface”两份规格接入同一条真实 DOCX 流程：

```text
普通 DOCX
→ 安全读取与字段图谱
→ OOXML 原结构导入小规文档工作表面（正文、基础表格、页眉页脚和浏览器可显示图片）
→ 业务字段和风险位置显示为黄色背景
→ 右侧业务字段 / 待确认问题
→ 点击黄色内容与右侧项目双向联动
→ 保存本机工作副本并可恢复
```

以下三个位置统一使用 `DocumentSurface` 接缝：

- 模板草稿复核；
- 正式模板生成前预览；
- 本机模板库预览。

模板复核、生成前预览和模板库预览统一使用 Univer 文档界面，不再向用户提供
“工作表面（试验）/原版式兼容视图”切换。Univer 启动失败时显示明确错误和重新载入，
不再静默切回 `docx-preview`。

## 如何启动

在本工作树执行：

```powershell
npm run dev:office-test
```

单机测试数据和工作副本放在 `D:\CodexTemp\xiaogui-office-surface-worktrees`，不把大体积缓存写入 C 盘。

## 建议测试步骤

1. 打开一条已经生成模板草稿的 WORK 对话，点击“直接打开文档复核”。
2. 确认直接显示唯一的 Univer 文档界面，页面上没有两种视图切换。
3. 用鼠标滚轮连续向下翻阅，确认能看到不同页面段，并保留正文的字体、字号、颜色、居中、基本表格结构和文中图片。
4. 点击文档中的黄色内容，确认右侧自动切换到对应业务字段或待确认问题；再从右侧点击项目，确认文档反向定位。
5. 切换“待确认”，确认复杂图片、文本框和浮动对象仍由右侧结构化界面处理。
6. 在工作表面修改少量文字，等待自动保存或点击“保存工作副本”。
7. 关闭复核器后再次打开，确认工作副本可以恢复。
8. 点击“稍后继续”，确认对话报告下方出现“继续复核”；点击后直接恢复同一份草稿。

## 当前边界

- 当前 Univer 开源版没有正式 DOCX Exchange；小规自行把 OOXML 段落、文字样式、基本表格、页面参数及页眉页脚映射为 Univer 文档，不宣称与 Microsoft Word 像素级一致。
- PNG、JPEG、GIF、BMP、WebP 图片已通过 Univer 官方绘图插件进入同一文档画布；浮动图片按原锚点近似定位。
- EMF、WMF、SVG、TIFF、组合图形、文本框位置和复杂环绕尚未完整还原；界面会显示数量和限制，并把需要处理的对象留在右侧清单，不把缺失内容冒充成成功导入。
- 本版不直接覆盖原 DOCX，也未开放正式 DOCX 导出。
- Office 网关只监听本机回环地址；文档快照接口通过消息通道传入的临时令牌授权，令牌不进入 URL、模型上下文或公开工具结果。

## 已完成的聚焦验证

- TypeScript 类型检查；
- 本机网关授权与工作副本重启恢复；
- 真实 DOCX 结构投影与字段位置映射；
- 业务字段使用实际黄色背景显示，点击黄色内容可切换右侧项目；
- 真实复杂 DOCX 的 22 个图片对象已进入 Univer 绘图资源并可在画布中显示；
- 鼠标滚轮可以连续浏览后续页面段；
- Office Surface 浏览器保存 / 重载冒烟；
- Electron 可见冒烟，并在超过原 15 秒超时窗口后确认未误降级。

本轮可见证据：

- `D:\CodexTemp\xiaogui-univer-images-highlight-final.png`：正文与真实黄色字段；
- `D:\CodexTemp\xiaogui-univer-canvas-image-centered.png`：同一 Univer 画布内的原文图件；
- `D:\CodexTemp\xiaogui-univer-images-scroll-final.png`：滚轮翻阅后的后续正文。

完整规格完成度见 `TWO_SPECS_SINGLE_MACHINE_AUDIT.md`。本报告证明的是“最新单机测试切片可用”，不代表两份规格定义的生产 V1 已全部完成。
