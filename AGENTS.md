# 小规 Agent 仓库规则

## Pi 原生、Skill 与插件优先门（强制）

每开发一个功能前，先实查以下复用顺序，并记录候选与验证结论：

1. Pi 原生能力；
2. 已安装或可引入的 Pi Skill；
3. Pi 插件、Extension、Package（包括 `pi.dev/packages` 中的候选）；
4. 已批准的成熟开源实现或院内既有能力；
5. 以上均不能满足已确认需求时，才允许提出最小框架修改。

只要 Pi 原生、Skill 或插件能够完成任务，就采用该能力，固定来源与版本，并随安装包或批准的安装流程交付；框架层不重复开发。验证候选时优先跑一条真实任务冒烟，不以名称、README 或模型推测代替实测。

框架修改方案必须同时给出：已检查的 Pi/Skill/插件候选、真实验证结果、不能满足的具体契约、为何适配仍不足，以及最小修改接缝。缺少这些证据时，停止框架施工并先完成复用调查。

## 模型与工具职责

- 复用 Pi 原生 Resource Loader、Skill 发现、插件装配、Agent Loop 和工具调用机制，让模型根据用户意图及清晰的 Skill/插件/工具说明选择能力。
- Skill/插件负责流程指导与能力组合；真正读取或修改本机资源的工具负责输入校验、权限、安全门和明确错误。
- 文件扩展名等信息只作为模型选择提示；工具必须校验自己实际接收的文件结构。
- 中央文档类型路由、通用文件令牌平台和自定义纠错状态机不是默认基础设施。只有单独批准且有真实缺口证据时才可建设。
- 已存在的专用私有文件交接只服务其原有安全流程，不自动上升为全局框架。

## Skill 与插件交付

- 优先使用 Pi 原生目录、Resource Loader 与插件机制，不另建平行加载器、市场、注册表或 Agent Loop。
- 下载或内置第三方 Skill/插件时，记录官方来源、精确版本或 commit、许可证与必要的安全审查；生产安装包不得依赖未固定的 `latest`。
- 自编 Skill 应把适用场景、可用工具、输入限制、失败处理和完成标准写清楚，并复用现有工具。
- 完成标准包括：全新安装或干净配置下能发现 Skill/插件，模型能通过真实任务选择并调用它，工具校验和错误路径可验证。

涉及新功能、Skill、插件、工具、Capability、文件处理或框架改动时，必须先阅读 [Pi 原生、Skill 与插件优先 ADR](doc/adr/ADR-PI-NATIVE-SKILL-PLUGIN-FIRST.md)。
